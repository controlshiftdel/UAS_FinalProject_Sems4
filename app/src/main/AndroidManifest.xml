<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.application">



    <application
        android:allowBackup="true"
        android:icon="@drawable/dompeticon"
        android:label="Khilaf Wealth Care"
        android:roundIcon="@drawable/dompeticon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PdfActivity"></activity>
        <activity android:name=".PengeluaranActivity" />
        <activity android:name=".NganuActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".viewholder.SplashScreenActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.HomeActivity" />
        <activity android:name=".activities.ProfileActivity" />
        <activity android:name=".activities.CreateActivity" />
    </application>

</manifest>